services:
  - type: web
    name: qx-auto-trade
    runtime: node
    plan: free
    buildCommand: npm install --prefix backend
    startCommand: node backend/src/server.js
    envVars:
      - key: NODE_ENV
        value: production
